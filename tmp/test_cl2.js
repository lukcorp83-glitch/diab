const axios = require("axios"); const { wrapper } = require("axios-cookiejar-support"); const { CookieJar } = require("tough-cookie"); async function testLogin() { const jar = new CookieJar(); const client = wrapper(axios.create({ jar: jar, withCredentials: true })); let r1 = await client.get("https://carelink.minimed.eu/patient/sso/login?country=pl&lang=pl"); let content = r1.data.toString(); require("fs").writeFileSync("tmp/auth0.html", content); } testLogin().catch(console.error);